import DarkModeButton from '~/components/DarkModeButton';

const Header = () => (
    <header class='grid w-screen auto-cols-fr grid-rows-1 text-center'>
        <DarkModeButton />
    </header>
);

export default Header;
